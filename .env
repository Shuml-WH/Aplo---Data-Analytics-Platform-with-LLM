# Host ports (change if conflicts with local services)
HOST_BACKEND_PORT=5001
HOST_FRONTEND_PORT=3000

# Ollama URL: uses local Ollama first, falls back to Docker Ollama
# Set to "http://ollama:11434" to skip local and always use Docker's
OLLAMA_BASE_URL=http://host.docker.internal:11434
